using System;

namespace fileManager
{
    class Program
    {
        static void Main(string[] args)
        {
            while (true){
                saveFile saveFile = new saveFile();
            }
        }
    }
}
